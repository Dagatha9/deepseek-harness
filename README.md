# Sổ tiêm chủng điện tử — bé Đặng Anh Vũ

Nhánh này chỉ dùng để lưu trữ GitHub Pages (Settings → Pages → Deploy from a branch → `gh-pages` → `/root`). Mã nguồn chính nằm ở nhánh `claude/vaccination-record-app-qpmdjg`, thư mục `vaccination-record/`.
